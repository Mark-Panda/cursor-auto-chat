{
  "name": "cursor-auto-chat",
  "displayName": "Cursor Auto Chat",
  "description": "Auto fill and submit cursor chat via local HTTP API",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Cursor Auto Chat",
      "properties": {
        "cursorAutoChat.port": {
          "type": "number",
          "default": 3777,
          "description": "The port for the local HTTP server"
        },
        "cursorAutoChat.target": {
          "type": "string",
          "enum": ["auto", "composer", "chat"],
          "default": "auto",
          "description": "auto: try Cursor Composer (Agent) first, then VS Code Chat; composer / chat: only that UI"
        },
        "cursorAutoChat.fallbackMacOsPaste": {
          "type": "boolean",
          "default": true,
          "description": "macOS only: activate Cursor and simulate Cmd+V for Lexical Composer. Requires System Settings → Privacy & Security → Accessibility (Cursor / Cursor Helper) and possibly Automation → allow Cursor to control System Events; quit Cursor fully after changing."
        },
        "cursorAutoChat.fallbackMacOsSubmit": {
          "type": "boolean",
          "default": true,
          "description": "macOS only: when built-in submit commands (composer.submit, etc.) all fail, simulate pressing Enter via System Events — same Accessibility / Automation permissions as fallbackMacOsPaste."
        },
        "cursorAutoChat.preferAgentMode": {
          "type": "boolean",
          "default": true,
          "description": "When using Composer, try to switch to Agent mode (not Ask) via workbench.action.chat.toggleAgentMode before filling."
        },
        "cursorAutoChat.serveOnlyWhenFocused": {
          "type": "boolean",
          "default": false,
          "description": "When true, start the HTTP server only while this window is focused; stops when you switch to another window/app so multiple Cursor windows can share the same port. External curl/scripts need this window focused (or Cursor in foreground), or set false for always-on."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vsix": "vsce package --allow-missing-repository",
    "package-extension": "npm run vsix"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.2",
    "typescript": "^5.3.3"
  }
}
